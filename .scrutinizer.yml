tools:
    external_code_coverage: true

filter:
    paths:
        - src/

checks:
    php: true

build:
    nodes:
        analysis:
            environment:
                php:
                    version: 7.4
            tests:
                override:
                    - php-scrutinizer-run
                    - phpcs-run
