tools:
    external_code_coverage: true

filter:
    paths:
        - src/
    dependency_paths:
        - vendor/

checks:
    php: true

build:
    environment:
        php:
            version: 7.4
    nodes:
        analysis:
            project_setup:
                override: true
            tests:
                override:
                    - php-scrutinizer-run
                    - phpcs-run
